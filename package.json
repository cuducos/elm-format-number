{
  "name": "elm-format-number",
  "version": "0.0.0",
  "scripts": {
    "pretest": "rm -rf tests/Doc tests/elm-stuff tests/Main.elm tests/Tests.elm",
    "test": "elm-verify-examples && elm-test init && rm tests/Example.elm && elm-test"
  },
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-test": "^0.18.7",
    "elm-verify-examples": "^1.0.2"
  }
}
